<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @product.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<div id="showReview">
  <%= render :partial => 'products/review', :locals => {:product_reviews => @product_reviews} %>
</div>

</div>
<% if user_signed_in? and (can? :create, @product.product_reviews.first)%>
  <div id="prod_rev_form" class="container">
    <%= render :partial => "product_reviews/form"%>
  </div>
<%end%>